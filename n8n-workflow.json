{
  "name": "Intent Spymaster — B2B Intent Research",
  "nodes": [
    {
      "id": "a1234567-0000-0000-0000-000000000001",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [0, 0],
      "webhookId": "intent-spymaster-v1",
      "parameters": {
        "httpMethod": "POST",
        "path": "intent-research",
        "responseMode": "responseNode",
        "options": {}
      }
    },
    {
      "id": "a1234567-0000-0000-0000-000000000002",
      "name": "Validate Inputs",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [240, 0],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Accept body from Webhook (handles both n8n body-wrapping styles)\nconst raw = $input.first().json;\nconst body = raw.body && typeof raw.body === 'object' ? raw.body : raw;\n\nconst topic         = (body.topic         || '').trim();\nconst keyword       = (body.keyword       || '').trim();\nconst audience      = (body.audience      || '').trim();\nconst clientWebsite = (body.clientWebsite || '').trim();\nconst provider      = (body.provider      || 'anthropic').trim();\n\nif (!topic)   throw new Error('topic is required');\nif (!keyword) throw new Error('keyword is required');\n\nreturn [{ json: { topic, keyword, audience, clientWebsite, provider } }];"
      }
    },
    {
      "id": "a1234567-0000-0000-0000-000000000003",
      "name": "Gather Data",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [480, 0],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Runs 5 Serper searches + homepage fetch in parallel.\n// Requires SERPER_API_KEY set in n8n → Settings → Environment Variables.\nconst { topic, keyword, audience, clientWebsite, provider } = $input.first().json;\nconst SERPER_KEY = $env.SERPER_API_KEY;\n\nconst queries = [\n  { source: 'Reddit',     query: `site:reddit.com \"${topic}\" \"${keyword}\" B2B OR software OR tool` },\n  { source: 'G2',         query: `site:g2.com/reviews \"${topic}\" \"${keyword}\"` },\n  { source: 'Capterra',   query: `site:capterra.com \"${topic}\" \"${keyword}\"` },\n  { source: 'Trustpilot', query: `site:trustpilot.com \"${topic}\" \"${keyword}\"` },\n  { source: 'Forum',      query: `\"${topic}\" \"${keyword}\" community OR forum questions pain points` },\n];\n\nasync function serperSearch(query, source) {\n  try {\n    const res = await fetch('https://google.serper.dev/search', {\n      method: 'POST',\n      headers: { 'X-API-KEY': SERPER_KEY, 'Content-Type': 'application/json' },\n      body: JSON.stringify({ q: query, num: 3 }),\n    });\n    const data = await res.json();\n    return (data.organic || []).slice(0, 3).map(r => ({\n      title:   r.title   || '',\n      snippet: r.snippet || '',\n      source,\n    }));\n  } catch {\n    return [];\n  }\n}\n\nasync function fetchWebsite(url) {\n  if (!url) return null;\n  try {\n    const res = await fetch(url, {\n      headers: {\n        'User-Agent': 'Mozilla/5.0 (compatible; IntentSpymasterBot/1.0)',\n        'Accept': 'text/html',\n      },\n    });\n    if (!res.ok) return null;\n    return await res.text();\n  } catch {\n    return null;\n  }\n}\n\nconst [snippetGroups, websiteHtml] = await Promise.all([\n  Promise.all(queries.map(({ query, source }) => serperSearch(query, source))),\n  fetchWebsite(clientWebsite),\n]);\n\nconst snippets = snippetGroups.flat().slice(0, 15);\n\nreturn [{ json: { topic, keyword, audience, clientWebsite, provider, snippets, websiteHtml } }];"
      }
    },
    {
      "id": "a1234567-0000-0000-0000-000000000004",
      "name": "Analyze Client Website",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [720, 0],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Extracts meaningful text from the fetched homepage HTML,\n// then asks the selected AI to identify the client's purpose, personas, and problems.\n// Silently skips if no website was provided or fetch failed.\nconst { topic, keyword, audience, clientWebsite, provider, snippets, websiteHtml } = $input.first().json;\n\n// ── HTML text extractor ────────────────────────────────────────────────────────\nfunction extractTextFromHTML(html) {\n  if (!html) return null;\n\n  function getText(regex) {\n    const m = html.match(regex);\n    return m ? m[1].replace(/<[^>]+>/g, ' ').trim() : '';\n  }\n\n  const title       = getText(/<title[^>]*>([\\s\\S]*?)<\\/title>/i);\n  const description = getText(/<meta[^>]+name=[\"']description[\"'][^>]+content=[\"']([^\"']+)[\"']/i)\n                   || getText(/<meta[^>]+content=[\"']([^\"']+)[\"'][^>]+name=[\"']description[\"']/i);\n\n  const headings = [...(html.matchAll(/<h[1-3][^>]*>([\\s\\S]*?)<\\/h[1-3]>/gi))]\n    .map(m => m[1].replace(/<[^>]+>/g, ' ').trim())\n    .filter(Boolean).slice(0, 10).join(' · ');\n\n  const paragraphs = [...(html.matchAll(/<p[^>]*>([\\s\\S]*?)<\\/p>/gi))]\n    .map(m => m[1].replace(/<[^>]+>/g, ' ').trim())\n    .filter(t => t.length > 40).slice(0, 12).join(' ');\n\n  const listItems = [...(html.matchAll(/<li[^>]*>([\\s\\S]*?)<\\/li>/gi))]\n    .map(m => m[1].replace(/<[^>]+>/g, ' ').trim())\n    .filter(t => t.length > 20).slice(0, 10).join(' • ');\n\n  return [title, description, headings, paragraphs, listItems]\n    .filter(Boolean).join('\\n').replace(/\\s+/g, ' ').trim().slice(0, 3000);\n}\n\n// ── AI caller ──────────────────────────────────────────────────────────────────\nasync function callAI(prompt, provider, maxTokens) {\n  if (provider === 'openai') {\n    const res = await fetch('https://api.openai.com/v1/chat/completions', {\n      method: 'POST',\n      headers: {\n        'Authorization': `Bearer ${$env.OPENAI_API_KEY}`,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        model: 'gpt-4o',\n        max_tokens: maxTokens,\n        messages: [{ role: 'user', content: prompt }],\n      }),\n    });\n    const data = await res.json();\n    if (data.error) throw new Error(data.error.message);\n    return data.choices[0].message.content;\n  } else {\n    const res = await fetch('https://api.anthropic.com/v1/messages', {\n      method: 'POST',\n      headers: {\n        'x-api-key': $env.ANTHROPIC_API_KEY,\n        'anthropic-version': '2023-06-01',\n        'content-type': 'application/json',\n      },\n      body: JSON.stringify({\n        model: 'claude-sonnet-4-20250514',\n        max_tokens: maxTokens,\n        messages: [{ role: 'user', content: prompt }],\n      }),\n    });\n    const data = await res.json();\n    if (data.error) throw new Error(data.error.message);\n    return data.content[0].text;\n  }\n}\n\nfunction parseJSON(raw) {\n  return JSON.parse(\n    raw.replace(/^```(?:json)?\\s*/i, '').replace(/\\s*```$/i, '').trim()\n  );\n}\n\n// ── Main ───────────────────────────────────────────────────────────────────────\nconst websiteText = extractTextFromHTML(websiteHtml);\nlet clientContext = null;\n\nif (websiteText) {\n  const prompt = `You are a B2B positioning analyst. Read the website copy below and extract a concise understanding of this company.\n\nReturn ONLY a valid JSON object — no explanation, no markdown, no preamble.\n\nFormat:\n{\n  \"purpose\": \"1–2 sentences: what they do, what gap they fill, what makes them unique\",\n  \"personas\": \"1–2 sentences: who their main target audiences or buyers are\",\n  \"problems\": \"1–2 sentences: the core pains or challenges they solve for those personas\"\n}\n\nRules:\n- Be specific — avoid generic marketing filler like \"we help businesses grow\"\n- If a field cannot be determined from the text, write \"Not clear from website\"\n- Keep each field to 1–2 sentences maximum\n\nWebsite copy:\n${websiteText}`;\n\n  try {\n    const raw = await callAI(prompt, provider, 512);\n    clientContext = parseJSON(raw);\n  } catch {\n    // silently degrade — research continues without client context\n  }\n}\n\nreturn [{ json: { topic, keyword, audience, clientWebsite, provider, snippets, clientContext } }];"
      }
    },
    {
      "id": "a1234567-0000-0000-0000-000000000005",
      "name": "Run Intent Analysis",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [960, 0],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Builds the master intent prompt (with optional client context),\n// calls the selected AI provider, and returns parsed intent signals.\nconst { topic, keyword, audience, provider, snippets, clientContext, clientWebsite } = $input.first().json;\n\nif (!snippets || snippets.length === 0) {\n  throw new Error('No snippets found — try a broader topic or keyword.');\n}\n\n// ── AI caller ──────────────────────────────────────────────────────────────────\nasync function callAI(prompt, provider, maxTokens) {\n  if (provider === 'openai') {\n    const res = await fetch('https://api.openai.com/v1/chat/completions', {\n      method: 'POST',\n      headers: {\n        'Authorization': `Bearer ${$env.OPENAI_API_KEY}`,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        model: 'gpt-4o',\n        max_tokens: maxTokens,\n        messages: [{ role: 'user', content: prompt }],\n      }),\n    });\n    const data = await res.json();\n    if (data.error) throw new Error(data.error.message);\n    return data.choices[0].message.content;\n  } else {\n    const res = await fetch('https://api.anthropic.com/v1/messages', {\n      method: 'POST',\n      headers: {\n        'x-api-key': $env.ANTHROPIC_API_KEY,\n        'anthropic-version': '2023-06-01',\n        'content-type': 'application/json',\n      },\n      body: JSON.stringify({\n        model: 'claude-sonnet-4-20250514',\n        max_tokens: maxTokens,\n        messages: [{ role: 'user', content: prompt }],\n      }),\n    });\n    const data = await res.json();\n    if (data.error) throw new Error(data.error.message);\n    return data.content[0].text;\n  }\n}\n\n// ── Prompt builder ─────────────────────────────────────────────────────────────\nconst audienceClause = audience\n  ? `\\nAudience filter: Focus signals relevant to \"${audience}\". Deprioritize signals not applicable to this persona.`\n  : '';\n\nconst clientClause = clientContext\n  ? `\\nClient context (use this to judge relevance — prioritise signals this client could credibly address):\\n- Purpose: ${clientContext.purpose}\\n- Serves: ${clientContext.personas}\\n- Solves: ${clientContext.problems}`\n  : '';\n\nconst formattedSnippets = snippets\n  .map((s, i) => `[${i + 1}] Source: ${s.source}\\nTitle: ${s.title}\\nSnippet: ${s.snippet}`)\n  .join('\\n\\n');\n\nconst prompt = `You are a B2B content strategist helping writers understand real audience intent.\n\nBelow are search result snippets about \"${topic}\" (keyword: \"${keyword}\"). Your job is to extract genuine user intent signals that a writer could use to create better B2B content.${audienceClause}${clientClause}\n\nRules:\n- Only extract signals that are present in the snippets. Do not invent or infer beyond what is written.\n- Focus on signals relevant to a B2B audience (buyers, practitioners, decision-makers).\n- Skip consumer-only content, spam, or irrelevant results.\n- If client context is provided, deprioritise signals that have no plausible connection to what that client does.\n- Write each signal as a clear, natural statement or question (1–2 sentences max).\n- Return a maximum of 15 signals total.\n\nAssign each signal one category:\n- Question — something users are actively trying to figure out\n- Pain Point — a frustration, failure, or unmet need\n- Comparison — evaluating options, switching decisions, or vendor comparisons\n- Anecdote — a personal story or real-world use case\n- Feature Request — something users wish a product or solution would do\n\nReturn ONLY a valid JSON array. No explanation, no markdown, no preamble.\n\nFormat:\n[\n  {\n    \"signal\": \"...\",\n    \"category\": \"Pain Point\",\n    \"source\": \"Reddit\"\n  }\n]\n\nSnippets:\n${formattedSnippets}`;\n\n// ── Run ────────────────────────────────────────────────────────────────────────\nconst raw = await callAI(prompt, provider, 1024);\nconst signals = JSON.parse(\n  raw.replace(/^```(?:json)?\\s*/i, '').replace(/\\s*```$/i, '').trim()\n);\n\nreturn [{\n  json: {\n    signals,\n    clientContext,\n    snippetCount: snippets.length,\n  }\n}];"
      }
    },
    {
      "id": "a1234567-0000-0000-0000-000000000006",
      "name": "Respond to Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [1200, 0],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ $input.first().json }}",
        "options": {
          "responseCode": 200,
          "responseHeaders": {
            "entries": [
              { "name": "Content-Type", "value": "application/json" }
            ]
          }
        }
      }
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [{ "node": "Validate Inputs", "type": "main", "index": 0 }]
      ]
    },
    "Validate Inputs": {
      "main": [
        [{ "node": "Gather Data", "type": "main", "index": 0 }]
      ]
    },
    "Gather Data": {
      "main": [
        [{ "node": "Analyze Client Website", "type": "main", "index": 0 }]
      ]
    },
    "Analyze Client Website": {
      "main": [
        [{ "node": "Run Intent Analysis", "type": "main", "index": 0 }]
      ]
    },
    "Run Intent Analysis": {
      "main": [
        [{ "node": "Respond to Webhook", "type": "main", "index": 0 }]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "callerPolicy": "workflowsFromSameOwner",
    "errorWorkflow": ""
  },
  "pinData": {},
  "versionId": "a1234567-0000-0000-0000-000000000099",
  "meta": {
    "templateCredsSetupCompleted": true
  },
  "tags": [
    { "name": "intent-research", "createdAt": "2025-01-01T00:00:00.000Z" }
  ]
}
